#include <iostream>
#include <algorithm>
#include <vector>
#include <set>
#include <iomanip>
#include <cmath>
#include <string>

#define REP(i, n) for(long long i=0;i<(n);i++)
#define LL long long
using namespace std;
const double PI = acos(-1);

int main() {
    //template
    ios::sync_with_stdio(false);
    cout << setprecision(20) << fixed; // default is 6

    return 0;
}
