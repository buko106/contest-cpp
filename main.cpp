#include <iostream>
#include <algorithm>
#include <vector>

#define REP(i,n) for(long long i=0;i<(n);i++)
#define LL long long
using namespace std;

int main(void) {
   // template
   ios::sync_with_stdio(false);

   return 0;
}